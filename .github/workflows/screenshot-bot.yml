# .github/workflows/screenshot-bot.yml
on:
  workflow_run:
    workflows: [⚙️ E2E testing]
    types: [requested, completed]
  pull_request:
    types: [closed]

jobs:
  awake-screenshot-bot:
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: write
      pull-requests: write
    steps:
      - uses: taiga-family/argus@optional-workflowWithTests-config
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
