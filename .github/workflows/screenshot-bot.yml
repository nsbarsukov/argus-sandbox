name: screenshot-bot.yml
on:
  workflow_run:
    types: [requested, completed]
  pull_request:
    types: [closed]

jobs:
  awake-screenshot-bot:
    runs-on: ubuntu-latest
    steps:
      - uses: taiga-family/argus@dc8aeb28646132c7b7ba89084c04fa679a58c322
